{
  "name": "ongkir-sdk",
  "private": true,
  "workspaces": ["packages/*", "examples/*", "apps/*"],
  "scripts": {
    "build": "bun run --filter @ongkir-sdk/core build && bun run --filter @ongkir-sdk/biteship build && bun run --filter @ongkir-sdk/komerce build && bun run --filter @ongkir-sdk/shipper build && bun run --filter @ongkir-sdk/cache-memory build && bun run --filter @ongkir-sdk/hono build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "bun run --filter './packages/*' typecheck",
    "test": "bun run --filter './packages/*' test",
    "changeset": "changeset",
    "changeset:enter": "changeset pre enter",
    "changeset:exit": "changeset pre exit",
    "version": "changeset version",
    "publish": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@changesets/cli": "^2.28.0",
    "typescript": "^5.7.0"
  }
}
